global:
  scrape_interval: 15s
  scrape_timeout: 10s
  evaluation_interval: 30s

rule_files:
  - /etc/prometheus/rules/*.yml

scrape_configs:
  - job_name: hybrid-agent
    metrics_path: /metrics
    static_configs:
      - targets: ['api:8000']
        labels:
          service: api
